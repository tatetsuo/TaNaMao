{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "yarn build",
  "build": {
    "env": {
      "YARN": "true",
      "NPM": "true"
    }
  }
}
